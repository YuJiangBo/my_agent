spring:
  ai:
    mcp:
      server:
        name: my_agent_mcp_server
        version: 0.0.1
        type: SYNC
        # stdio
        stdio: true
  # stdio
  main:
    web-application-type: none
    banner-mode: off
